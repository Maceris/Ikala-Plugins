package com.ikalagaming.factory.networking.base;

/** The direction that a packet is traveling towards. */
public enum RequestDirection {
    /** To a client from a server. */
    CLIENT_BOUND,
    /** To a server from a client. */
    SERVER_BOUND
}
